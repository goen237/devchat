# Test Environment Configuration
NODE_ENV=test

# Test Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME_TEST=devchat_test

# JWT Secret (Test)
JWT_SECRET=test-secret-key-for-testing-only

# Redis (optional für Tests)
REDIS_HOST=localhost
REDIS_PORT=6379
